<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mongo="http://www.springframework.org/schema/data/mongo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo.xsd">

	<mongo:mongo-client id="nocredmongofri" replica-set="${mongodb.replication.set}" >
		<mongo:client-options max-connection-idle-time="${mongodb.max.connection.idle:300000}" />
	</mongo:mongo-client>

	<mongo:db-factory id="noCredMongoDbFactory" dbname="${mongodb.dbname}" mongo-ref="nocredmongofri" />
	
	<mongo:mongo-client id="mongofri" replica-set="${mongodb.replication.set}" credentials="${MONGO_USER}@${mongodb.dbname}" >
		<mongo:client-options max-connection-idle-time="${mongodb.max.connection.idle:300000}" />
	</mongo:mongo-client>

	<mongo:db-factory id="mongoDbFactory" dbname="${mongodb.dbname}" mongo-ref="mongofri" />

	<bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
		<constructor-arg ref="${mongodb.factory}" />
		<constructor-arg ref="mongoConverter" />
	</bean>

	<bean id="mongoConverter" class="org.springframework.data.mongodb.core.convert.MappingMongoConverter">
		<constructor-arg ref="${mongodb.factory}" />
		<constructor-arg>
			<bean class="org.springframework.data.mongodb.core.mapping.MongoMappingContext" />
		</constructor-arg>
		<property name="mapKeyDotReplacement" value="%2E" />
	</bean>

	<mongo:repositories base-package="com.fri.mxx.repository.ctv2" repository-impl-postfix="CustomImpl" />

</beans>